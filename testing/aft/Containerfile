FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY aft_simulator.py .

# All tunables come from env (see aft_simulator.py docstring).
ENTRYPOINT ["python", "aft_simulator.py"]
