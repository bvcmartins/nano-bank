meta {
  name: Reverse
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/transactions/{{depositTxnId}}/reverse
  body: json
  auth: inherit
}

body:json {
  {
    "reason": "customer dispute"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
