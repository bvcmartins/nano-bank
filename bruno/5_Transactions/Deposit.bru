meta {
  name: Deposit
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/transactions/deposit
  body: json
  auth: inherit
}

body:json {
  {
    "account_id": "{{chequingId}}",
    "amount": 100.00,
    "description": "Initial deposit"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
