meta {
  name: Transfer
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/transactions/transfer
  body: json
  auth: inherit
}

body:json {
  {
    "from_account_id": "{{chequingId}}",
    "to_account_id": "{{savingsId}}",
    "amount": 25.00,
    "description": "Move to savings"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
