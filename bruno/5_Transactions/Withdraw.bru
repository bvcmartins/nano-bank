meta {
  name: Withdraw
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/transactions/withdrawal
  body: json
  auth: inherit
}

body:json {
  {
    "account_id": "{{chequingId}}",
    "amount": 30.00,
    "description": "ATM withdrawal"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
